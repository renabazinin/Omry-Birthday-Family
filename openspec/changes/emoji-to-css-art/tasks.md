## 1. CSS Balloons

- [x] 1.1 Replace 8 emoji balloon `<span>` elements in HTML with `<div class="balloon">` elements (empty, CSS will draw them)
- [x] 1.2 Style `.balloon` as a colored circle with `::before` for knot triangle and `::after` for string line
- [x] 1.3 Assign 8 distinct colors from the palette to balloon-1 through balloon-8
- [x] 1.4 Verify floatUp animation still works with the new div-based balloons

## 2. CSS Decorations

- [x] 2.1 Replace top decoration emojis (⭐🎂⭐) with CSS shapes: stars via clip-path, circle for center
- [x] 2.2 Replace bottom decoration emojis (🎁🎊🎁) with CSS shapes: diamonds (rotated squares), circle for center
- [x] 2.3 Replace 🎉🎉 party hats around name with small stacked colored dots using pseudo-elements on `.name-badge`
- [x] 2.4 Remove the `<span class="party-hat">` elements from HTML, add dot accent elements or use pseudo-elements

## 3. CSS Icons

- [x] 3.1 Replace bubble emoji icons (📅🕐📍) with `<span>` containing Hebrew letters (י, ש, מ) inside colored circles
- [x] 3.2 Style `.bubble-icon` as small colored circles with centered Hebrew letter, matching each bubble's color scheme
- [x] 3.3 Replace 💛 in RSVP text with a CSS heart shape using `::after` pseudo-element
- [x] 3.4 Replace 🔊 in sound button with a CSS-drawn speaker shape (rectangle + triangle borders)

## 4. Cleanup & Verify

- [x] 4.1 Search for any remaining emoji characters in HTML and remove them
- [x] 4.2 Verify all shapes render correctly on mobile viewport
- [x] 4.3 Verify animations (sparkle, sway, float) still work with new CSS elements
